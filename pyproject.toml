[project]
name = "local-realtime-llm"
version = "0.1.0"
description = "OpenAI Realtime API (WebRTC) compatible local server, MLX backend"
requires-python = "==3.12.*"
dependencies = [
    "speech-to-speech[webrtc]==0.2.12",
]

[tool.uv]
package = false
